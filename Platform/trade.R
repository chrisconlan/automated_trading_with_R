# First exit these
toExit <- read.csv(file = "stocksToExit.csv")

# Then enter these
toEnter <- read.csv(file = "stocksToEnter.csv")

# This is open-ended...
# This may be done inside or outside R depending on choice of brokerage and AP
